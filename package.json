{
  "name": "shopsense",
  "private": true,
  "version": "1.0.0",
  "description": "Runs the ShopSense API and static frontend together for local development.",
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --prefix server\" \"serve shopsense -l 5500\"",
    "install:all": "npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "serve": "^14.2.4"
  }
}
